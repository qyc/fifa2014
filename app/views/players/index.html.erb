<h1>Players</h1>

<table>
  <thead>
    <tr>
	  <th>Name</th>
	  <th>E-mail</th>
    </tr>
  </thead>

  <tbody>
    <% @players.each do |player| %>
      <tr>
		<td><%= player.name %></td>
		<td><%= player.email %></td>
      </tr>
    <% end %>
  </tbody>
</table>
